name: Atualizar dados SUSEP

on:
  schedule:
    - cron: '0 9 * * 1'  # toda segunda-feira às 9h UTC (4h BRT)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write # <--- ADICIONE ESTA LINHA AQUI
    steps:
      - name: Clonar repositório
        uses: actions/checkout@v3

      - name: Configurar Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Instalar dependências
        # Sugestão: Consolide as duas linhas de pip install em uma
        run: pip install pandas openpyxl xlrd requests certifi urllib3

      # - name: Instalar dependências Python # Esta linha pode ser removida se você consolidar acima
      #   run: pip install requests certifi pandas

      - name: Rodar script de atualização
        run: python scripts/main.py

      - name: Commit e push dos arquivos extraídos
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add data/*.csv || true
          git diff --cached --quiet || (git commit -m "Atualização automática dos dados SUSEP" && git push)
